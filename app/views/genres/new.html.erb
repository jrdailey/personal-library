<div id="new-genre-form-wrapper">
  <%= form_for(@genre, html: { class: 'form-horizontal', id: 'new-genre-form' }) do |f| %>
    <% if @genre.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@genre.errors.count, "error") %> prohibited this genre from being saved:</h2>

        <ul>
        <% @genre.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group">
      <%= f.label :name, class: 'contol-label' %>
      <%= f.text_field :name, class: 'form-control' %>
    </div>

    <div class="actions text-center">
      <%= f.submit %>
    </div>
  <% end %>
  <div class="cancel-add-genre-wrapper">
    <a href="#" class="cancel-add-genre">cancel</a>
  </div>
</div>
